# MaltiOnTheGo — public content repo

Over-the-air lesson content (JSON + later audio) served via jsDelivr to the
MaltiOnTheGo app. Public so the CDN can read it; only the greenworksmalta account
+ collaborators can write. See manifest.json for the published content version.
App code lives in the private MalteseLessons repo.
